import { Meta } from '@storybook/addon-docs/blocks'

import { markdownToHTML, styleMarkdown } from '../utils.js'
import CHANGELOG from '../../../CHANGELOG.md'

<Meta title="Overview/Changelog" />

<div dangerouslySetInnerHTML={{__html: styleMarkdown(markdownToHTML(CHANGELOG))}}></div>
